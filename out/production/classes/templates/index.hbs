{{#partial "content"}}

    <h1>Team list!</h1>

    {{#if posts}}
        {{#each posts }}
            <p>Current team:</p>
            <p>{{ teamName }} <a href="/teams/{{id}}">read more...</a></p>
        {{/each}}
    {{else}}
        <p>No teams!</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}